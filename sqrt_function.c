#include<stdio.h>
#include<math.h>
int main(){
    int num;
    printf("enter the number of sqrt you want ");
    scanf("%d",&num);
    int result=sqrt(num);
    printf("the sqrt of entred value is %d",result);
    return 0;
}